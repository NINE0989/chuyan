"""Minimal getinput used for testing the injector.

Provides USER_INPUT and get_user_input(). When executed as a script it prints the
value to stdout so the injector can capture it.
"""

USER_INPUT = (
    "横向的线，从左到右，随着音乐起伏，艺术化展现傅里叶频谱，并有丰富的颜色变化"
)

